'use strict';

/**
 *  container-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::container-image.container-image');
